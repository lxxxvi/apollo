require 'application_system_test_case'

class PollsVoteTest < ApplicationSystemTestCase
  test 'guest votes for a started poll using valid token' do
    assert false, 'implement me'
  end

  test 'guest cannot vote for a poll using invalid token' do
    assert false, 'implement me'
  end

  test 'guest cannot vote for a unstarted poll, with valid token' do
    assert false, 'implement me'
  end
end
